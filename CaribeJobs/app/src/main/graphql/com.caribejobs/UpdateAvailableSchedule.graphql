mutation UpdateSchedule ($username: String!, $schedule: [AvailableScheduleInput]!) {
    updateUserAvailableSchedule(username: $username, schedule: $schedule) {
        responseMessage
        errorCode
    }
}